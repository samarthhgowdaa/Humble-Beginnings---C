# Variable

It is a reusable container for a value, which is used to represent some value.
We have many data-types which a variable can store.

To display a variable we need to use a *format specifier*.

## Data types

![img][dataTypes]

[dataTypes]: /assets/img/infoVariables.png